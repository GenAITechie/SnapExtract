// This file can be used for shared type definitions.
// For now, specific types like ExtractBillDataOutput are imported directly from AI flows.

export interface UserProfile {
  email: string | null;
}

// Add other shared types here as the application grows.
